# -*- coding: utf-8 -*-
"""LoanAmountPrediction.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1o3RaFhBFbd6PY-zwr5KpkKaEl0-9fP3g
"""

# Commented out IPython magic to ensure Python compatibility.
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
# %matplotlib inline
import warnings
warnings.filterwarnings('ignore')

from google.colab import files
dataset = files.upload()

pip install scikit-learn==1.4.2 numpy==1.19.5 scipy==1.13.0 joblib==1.4.0

df.describe()

df.isnull().sum()

dataset = pd.read_csv('dataset_updated.csv')

# Now you can access the data using iloc

X = dataset.iloc[:, :-1].values
y = dataset.iloc[:, -1].values

from sklearn.model_selection import train_test_split
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size = 1/3, random_state = 0)

from sklearn.linear_model import LinearRegression
regressor = LinearRegression()
regressor.fit(X_train, y_train)

y_pred = regressor.predict(X_test)

regressor.predict(X_test)

regressor.predict([[3, 1, 0, 9600000, 29900000, 12, 778, 2400000, 17600000, 22700000, 8000000]])

import pickle
with open('LoanAmountPrediction.pkl','wb') as f:
    pickle.dump(regressor,f)

regressor.coef_

from google.colab import files

files.download('LoanAmountPrediction.pkl')

